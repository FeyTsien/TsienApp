<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.hdnz.inanming">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECEIVE_MMS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- 8.0打开apk，安装权限 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- &lt;!&ndash; 硬件加速对X5视频播放非常重要，建议开启 &ndash;&gt; -->
    <!-- <uses-permission android:name="android.permission.GET_TASKS" /> -->
    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:maxRecents="5"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup"
        tools:targetApi="m">
        <!--Android P 将禁止 App 使用所有未加密的连接，加上android:usesCleartextTraffic="true"可避免-->

        <!-- 服务器选择列表 -->
        <activity
            android:name=".ui.activity.SplashActivity"
            android:theme="@style/TranslucentTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <!-- &lt;!&ndash; 下载测试 &ndash;&gt; -->
        <!-- <activity android:name=".mvp.ui.activity.DownloadTestActivity" /> -->
        <!-- 登录 -->
        <activity
            android:name=".ui.activity.login.LoginActivity"
            android:launchMode="singleTop" />
        <!-- 密码登录 -->
        <activity
            android:name=".ui.activity.login.LoginPwdLoginActivity"
            android:launchMode="singleTop" />
        <!-- 密码登录 -->
        <activity
            android:name=".ui.activity.login.ForgetPasswordActivity"
            android:launchMode="singleTop" />
        <!-- 注册 -->
        <activity
            android:name=".ui.activity.login.LogUpActivity"
            android:launchMode="singleTop" />

        <!-- 主页 -->
        <activity
            android:name=".ui.activity.main.MainActivity"
            android:launchMode="singleTask" />
        <!-- 主页 -->
        <activity
            android:name=".ui.activity.main.MainActivity2"
            android:launchMode="singleTask" />
        <!-- 主页 -->
        <activity
            android:name=".ui.activity.main.MainActivity1"
            android:launchMode="singleTask" />

        <!--LinkActivity-->
        <activity
            android:name=".ui.activity.ApplinksActivity"
            android:alwaysRetainTaskState="true"
            android:launchMode="singleTask"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter
                android:autoVerify="true"
                tools:targetApi="m">
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <action android:name="android.intent.action.VIEW" />

                <data
                    android:host="link"
                    android:path="/qqq"
                    android:scheme="https" />
                <data
                    android:host="link"
                    android:path="/qqq"
                    android:scheme="http" />

            </intent-filter>
        </activity>

        <!-- WebView -->
        <activity
            android:name=".webview.WebViewActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:launchMode="singleTop"
            android:windowSoftInputMode="stateHidden|adjustResize" />

        <!-- 腾讯X5播放H5视频类 -->
        <activity
            android:name="com.tencent.smtt.sdk.VideoActivity"
            android:alwaysRetainTaskState="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="false"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="com.tencent.smtt.tbs.video.PLAY" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--==================首页===================================-->
        <!-- 搜索页 -->
        <activity
            android:name=".ui.activity.home.SearchActivity"
            android:launchMode="singleTop" />
        <!-- 自定义扫描 -->
        <activity
            android:name=".ui.activity.home.ScanActivity"
            android:launchMode="singleTop" />
        <!-- 政务办事 -->
        <activity
            android:name=".ui.activity.home.GovAffairsOfficeActivity"
            android:launchMode="singleTop" />
        <!-- 政务办事的详情俩列表（业务办理，办事指南） -->
        <activity
            android:name=".ui.activity.home.govaffairsoffice.GovAffairInfoActivity"
            android:launchMode="singleTop" />
        <!-- 办事指南—>事项详情 -->
        <activity
            android:name=".ui.activity.home.govaffairsoffice.govaffairinfo.ItemdetailsActivity"
            android:launchMode="singleTop" />
        <!-- 政务公开 -->
        <activity
            android:name=".ui.activity.home.GovAffairsPublicActivity"
            android:launchMode="singleTop" />
        <!-- 公开详情 -->
        <activity
            android:name=".ui.activity.home.govaffairspublic.GAPInfoActivity"
            android:launchMode="singleTop" />
        <!-- 政务预约 -->
        <activity
            android:name=".ui.activity.home.GovAffairsBookingActivity"
            android:launchMode="singleTop" />
        <!-- 政务预约-业务 -->
        <activity
            android:name=".ui.activity.home.govaffairsbooking.GABBusinessActivity"
            android:launchMode="singleTop" />
        <!-- 政务预约-业务-信息 -->
        <activity
            android:name=".ui.activity.home.govaffairsbooking.gabbusiness.GABBookingInfoActivity"
            android:launchMode="singleTop" />

        <!--==================工作台===================================-->
        <!-- 头条列表 -->
        <activity
            android:name=".ui.activity.headlinelist.HeadLineActivity"
            android:launchMode="singleTop" />
        <!-- 创建任务 -->
        <activity
            android:name=".ui.activity.workbench.CreateTaskActivity"
            android:launchMode="singleTop" />
        <!-- 任务设置 -->
        <activity
            android:name=".ui.activity.workbench.TaskSetActivity"
            android:launchMode="singleTop" />
        <!-- 任务详情 -->
        <activity
            android:name=".ui.activity.workbench.TaskInfoActivity"
            android:launchMode="singleTop" />

        <!--==================消息===================================-->
        <!-- 政务消息 -->
        <activity
            android:name=".ui.activity.message.GovernmentMessagesActivity"
            android:launchMode="singleTop" />
        <!-- 系统消息 -->
        <activity
            android:name=".ui.activity.message.SystemMessagesActivity"
            android:launchMode="singleTop" />

        <!--==================我的===================================-->
        <!-- 我的 -->
        <activity
            android:name=".ui.activity.me.MyInfoActivity"
            android:launchMode="singleTop" />
        <!-- 我的头像 -->
        <activity
            android:name=".ui.activity.me.information.MyPortraitActivity"
            android:launchMode="singleTop" />
        <!-- 我的—收藏页面 -->
        <activity
            android:name=".ui.activity.me.CollectActivity"
            android:launchMode="singleTop" />
        <!-- 我的—设置页面 -->
        <activity
            android:name=".ui.activity.setting.SettingActivity"
            android:launchMode="singleTop" />
        <!-- 我的—设置-修改密码、验证码修改密码页面 -->
        <activity
            android:name=".ui.activity.setting.alter.AlterPasswordActivity"
            android:launchMode="singleTop" />
        <!-- 我的—设置-修改手机号页面 -->
        <activity
            android:name=".ui.activity.setting.alter.AlterPhoneActivity"
            android:launchMode="singleTop" />
        <!-- 我的—个人信息-地址列表页面 -->
        <activity
            android:name=".ui.activity.me.information.MyAddressListActivity"
            android:launchMode="singleTop" />
        <!-- 我的—个人信息-添加地址、编辑地址页面 -->
        <activity
            android:name=".ui.activity.me.information.address.MyAddressActivity"
            android:launchMode="singleTop" />
        <!-- 关于我们页面 -->
        <activity
            android:name=".ui.activity.me.AboutUsActivity"
            android:launchMode="singleTop" />
        <!-- 身份证实名认证页面 -->
        <activity
            android:name=".ui.activity.certification.idCard.IdCardAuthenticationActivity"
            android:launchMode="singleTop" />
        <!-- 身份证实名认证页面 -->
        <activity
            android:name=".ui.activity.certification.idCard.IdCardAuthenticationActivityC"
            android:launchMode="singleTop" />
        <!-- 实名认证页面（包含4个隐藏界面） -->
        <activity
            android:name=".ui.activity.me.AuthenticationActivity"
            android:launchMode="singleTop" />
        <!-- 阿里实名认证页面（未实现） -->
        <activity
            android:name=".ui.activity.certification.alipay.AliAuthenticationActivity"
            android:launchMode="singleTop" />
        <!-- 我的证照列表页面 -->
        <activity
            android:name=".ui.activity.me.MyLicenseListActivity"
            android:launchMode="singleTop" />
        <!-- 我的证照详情页面 -->
        <activity
            android:name=".ui.activity.me.license.LicenseInfoActivity"
            android:launchMode="singleTop" />
        <!-- 我的证照详情页面（图片列表） -->
        <activity
            android:name=".ui.activity.license.detail.LicenseDetailActivity"
            android:launchMode="singleTop" />
        <!-- 夫妻双方身份证照 -->
        <activity
            android:name=".ui.activity.license.couple.CoupleIdCardActivity"
            android:launchMode="singleTop" />
        <!-- 夫妻双方合影照 -->
        <activity
            android:name=".ui.activity.license.couple.CouplePhotoActivity"
            android:launchMode="singleTop" />
        <!-- 学生证证照 -->
        <activity
            android:name=".ui.activity.license.student.StudentIdActivity"
            android:launchMode="singleTop" />
        <!-- 户口本证照 -->
        <activity
            android:name=".ui.activity.license.householdRegister.HouseholdRegisterActivity"
            android:launchMode="singleTop" />
        <!-- 免冠一寸证照 -->
        <activity
            android:name=".ui.activity.license.inchPhoto.InchPhotoActivity"
            android:launchMode="singleTop" />
        <!-- 结婚证证照 -->
        <activity
            android:name=".ui.activity.license.couple.MarriageActivity"
            android:launchMode="singleTop" />
        <!-- 各种文件、表证照 -->
        <activity
            android:name=".ui.activity.license.register.RegistrationFormActivity"
            android:launchMode="singleTop" />
        <!-- 我的办理 -->
        <activity
            android:name=".ui.activity.me.MyTransactionActivity"
            android:launchMode="singleTop" />
        <!-- 办理详情 -->
        <activity
            android:name=".ui.activity.transaction.detail.TransactionDetailActivity"
            android:launchMode="singleTop" />

        <!-- 办理进度详情 -->
        <activity
            android:name=".ui.activity.transaction.checkProgress.CheckProgressDetailActivity"
            android:launchMode="singleTop" />
        <!-- 我的积分 -->
        <activity
            android:name=".ui.activity.me.MyIntegralActivity"
            android:launchMode="singleTop" />

        <!-- 我的预约 -->
        <activity android:name=".ui.activity.me.MyReservationActivity"
            android:launchMode="singleTop" />
        <!-- ================================== 服务 Service================================ -->

        <!-- 下载Service -->
        <service android:name=".service.DownLoadService" />
        <!-- 定位Service -->
        <service android:name=".service.LocationService" />
        <!-- 初始化X5内核的Service -->
        <service android:name=".webview.X5NetService" />

        <service
            android:name="com.tencent.smtt.export.external.DexClassLoaderProviderService"
            android:label="dexopt"
            android:process=":dexopt" />

        <!-- =====================================极光推送相关=================================== -->


        <!-- User defined.  For test only  用户自定义的广播接收器 -->
        <receiver
            android:name=".jpush.MyReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.REGISTRATION" /> <!-- Required  用户注册SDK的intent -->
                <action android:name="cn.jpush.android.intent.MESSAGE_RECEIVED" /> <!-- Required  用户接收SDK消息的intent（自定义消息Push） -->
                <action android:name="cn.jpush.android.intent.NOTIFICATION_RECEIVED" /> <!-- Required  用户接收SDK通知栏信息的intent -->
                <action android:name="cn.jpush.android.intent.NOTIFICATION_OPENED" /> <!-- Required  用户打开通知栏的intent（不配置此项，点击默认打开的是程序的主Activity） -->
                <action android:name="cn.jpush.android.intent.CONNECTION" /> <!-- 接收网络变化 连接/断开 since 1.6.3 -->
                <category android:name="com.hdnz.inanming" />
            </intent-filter>
        </receiver>

        <!-- User defined.  For test only  用户自定义接收消息器,3.0.7开始支持,目前新tag/alias接口设置结果会在该广播接收器对应的方法中回调-->
        <receiver android:name=".jpush.MyJPushMessageReceiver">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.RECEIVE_MESSAGE" />
                <category android:name="com.hdnz.inanming" />
            </intent-filter>
        </receiver>

        <!-- ========================7.0访问文件所需配置========================== -->
        <!-- 文件提供者（用在选择图片） -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.hdnz.inanming.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>


        <!-- 第三方裁剪图片activity -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".ui.activity.me.MyOrderActivity"></activity>

        <!-- ========================MOb分享回调配置========================== -->

        <!--
        （3.1.0版本之后（包含3.1.0）才需要注册此Activity）如果集成QQ分享，或者使用QQ客户端来进行QQ空间的分享，
        须要在此处添加一个回调activity，对ACTION_VIEW事件的过滤器，其中的scheme是“tencent”前缀再开发者应用的加上appId。
        如果此Activity和过滤器不设置，则分享结束以后不能得到正确的回调。
        -->
        <!-- QQ分享必须配置上面的activity tencent后面的appid改成你自己的QQ的appid即可 -->
        <activity
            android:name="cn.sharesdk.tencent.qq.ReceiveActivity"
            android:launchMode="singleTask"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="tencent100371282" />
            </intent-filter>
        </activity>

        <!--&lt;!&ndash; 微信分享回调 &ndash;&gt;-->
        <!--<activity-->
        <!--android:name=".mobShare.wxapi.WXEntryActivity"-->
        <!--android:configChanges="keyboardHidden|orientation|screenSize"-->
        <!--android:exported="true"-->
        <!--android:screenOrientation="portrait"-->
        <!--android:theme="@android:style/Theme.Translucent.NoTitleBar" />-->

    </application>
</manifest>